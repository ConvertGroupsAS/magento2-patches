--- a/Api/ImageProcessor.php	2026-03-30 16:47:31
+++ b/Api/ImageProcessor.php	2026-03-30 16:47:44
@@ -18,6 +18,11 @@
 class ImageProcessor implements ImageProcessorInterface
 {
     /**
+     * Allowed image file extensions (polyshell mitigation APSB25-94)
+     */
+    private const ALLOWED_EXTENSIONS = ['jpg', 'jpeg', 'gif', 'png'];
+
+    /**
      * @var array
      */
     protected $mimeTypeExtensionMap = [
@@ -154,6 +159,7 @@
 
         try {
             $this->uploader->processFileAttributes($fileAttributes);
+            $this->uploader->setAllowedExtensions(self::ALLOWED_EXTENSIONS);
             $this->uploader->setFilesDispersion(true);
             $this->uploader->setFilenamesCaseSensitivity(false);
             $this->uploader->setAllowRenameFiles(true);
