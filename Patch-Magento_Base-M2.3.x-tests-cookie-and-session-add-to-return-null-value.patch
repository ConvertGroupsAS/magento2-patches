--- dev/tests/static/framework/Magento/CodeMessDetector/Rule/Design/CookieAndSessionMisuse.php	2019-03-15 04:56:30.000000000 +0300
+++ dev/tests/static/framework/Magento/CodeMessDetector/Rule/Design/CookieAndSessionMisuse.php	2019-06-11 12:33:20.000000000 +0300
@@ -77,7 +77,7 @@
         foreach ($class->getMethods(\ReflectionMethod::IS_PUBLIC) as $method) {
             if (preg_match('/^(after|around|before).+/i', $method->getName())) {
                 try {
-                    $argument = $method->getParameters()[0]->getClass();
+                    $argument = $method->getParameters() ? $method->getParameters()[0]->getClass() : null;
                 } catch (\Throwable $exception) {
                     //Non-existing class (autogenerated perhaps) or doesn't have an argument.
                     continue;
@@ -106,7 +106,7 @@
         foreach ($class->getMethods(\ReflectionMethod::IS_PUBLIC) as $method) {
             if (preg_match('/^(after|around|before).+/i', $method->getName())) {
                 try {
-                    $argument = $method->getParameters()[0]->getClass();
+                    $argument = $method->getParameters() ? $method->getParameters()[0]->getClass() : null;
                 } catch (\Throwable $exception) {
                     //Non-existing class (autogenerated perhaps) or doesn't have an argument.
                     continue;
